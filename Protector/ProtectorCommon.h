#pragma once

//#define PROTECTOR_DEVICE 0x8000

//#define IOCTL_PROTECTOR_SET_PATH CTL_CODE(PROTECTOR_DEVICE, \
//	0x800, METHOD_NEITHER, FILE_ANY_ACCESS)

/*
enum class RequestType : short {
	Add,
	Remove
};

struct PathToProtect {
	PVOID Path;
	RequestType Type;
};

struct ItemHeader {
	UNICODE_STRING PathToProtect;
};
*/
